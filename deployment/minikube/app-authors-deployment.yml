apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-authors
  labels:
    app: app-authors
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-authors
  template:
    metadata:
      labels:
        app: app-authors
    spec:
      containers:
        - name: app-authors
          image: henry170/app-authors:latest
          env:
            - name: QUARKUS_DATASOURCE_USERNAME
              value: postgres
            - name: QUARKUS_DATASOURCE_PASSWORD
              value: postgres
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://library.crnn8whpm2rl.us-east-1.rds.amazonaws.com:5432/postgres
            - name: QUARKUS_HTTP_PORT
              value: "8080"
            - name: CONSUL_HOST
              value: consul
            - name: CONSUL_PORT
              value: "8500"
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
          # Uncomment and modify the command block if you want to add init or wait-for-it scripts
          # command:
          #   - sh
          #   - -c
          #   - |
          #     apk add --no-cache curl bash &&
          #     curl -o /usr/local/bin/wait-for-it https://raw.githubusercontent.com/vishnubob/wait-for-it/master/wait-for-it.sh &&
          #     chmod +x /usr/local/bin/wait-for-it &&
          #     /usr/local/bin/wait-for-it base_de_datos:5432 -- java -jar /app/app.jar
---
apiVersion: v1
kind: Service
metadata:
  name: app-authors
spec:
  type: ClusterIP
  selector:
    app: app-authors
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080


##Levantar servicio
# kubectl apply -f app-authors-deployment.yml

## Eliminar servicio
#  kubectl delete all --allc